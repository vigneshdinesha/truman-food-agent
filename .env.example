# Copy this file to `.env` and paste your key. `.env` is gitignored.
OPENAI_API_KEY=sk-...

# Optional overrides (defaults are the cheapest working combo):
# FOOD_CHAT_MODEL=gpt-4o-mini
# FOOD_EMBED_MODEL=text-embedding-3-small
